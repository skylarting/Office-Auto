name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    paths:
      - "export_summary.py"
      - "excel_mapper.py"
      - "excel_mapping_tool.py"
      - "excel_mapping_qt.py"
      - "smart_template.py"
      - "smart_template_qt.py"
      - "build_qt_icon.py"
      - "assets/excel-mapper.ico"
      - "test_export_summary.py"
      - "test_excel_mapper.py"
      - "test_excel_mapping_qt.py"
      - "test_smart_template.py"
      - ".github/workflows/build-exe.yml"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout files
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install pyinstaller PySide6-Essentials Pillow openpyxl xlrd xlwt xlutils pywin32

      - name: Generate Qt application icon
        run: python build_qt_icon.py

      - name: Run tests
        run: python -m unittest -v

      - name: Build ExcelMappingQt EXE
        run: >
          python -m PyInstaller
          --noconfirm
          --clean
          --onefile
          --windowed
          --optimize 2
          --exclude-module tkinter
          --exclude-module _tkinter
          --exclude-module PySide6.QtConcurrent
          --exclude-module PySide6.QtDBus
          --exclude-module PySide6.QtNetwork
          --exclude-module PySide6.QtOpenGL
          --exclude-module PySide6.QtOpenGLWidgets
          --exclude-module PySide6.QtPrintSupport
          --exclude-module PySide6.QtSql
          --exclude-module PySide6.QtTest
          --exclude-module PySide6.QtXml
          --hidden-import smart_template
          --hidden-import smart_template_qt
          --icon assets/excel-mapper.ico
          --add-data "assets/excel-mapper.ico;assets"
          --name ExcelMappingQt
          excel_mapping_qt.py

      - name: Upload ExcelMappingQt EXE
        uses: actions/upload-artifact@v4
        with:
          name: ExcelMappingQt-Windows
          path: dist/ExcelMappingQt.exe
          if-no-files-found: error
